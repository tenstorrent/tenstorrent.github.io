name: Build and Deploy Sphinx Documentation

on:
  workflow_dispatch:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-24.04
    environment: github-pages
    env:
      # OpenSearch-backed docs search service (see scripts/index_remote_search.py).
      SEARCH_API_BASE: https://csl860x2oj.execute-api.us-east-2.amazonaws.com/prod
      DOC_CATALOG_SOURCE_ID: docs-tenstorrent
      DOC_SITE_BASE_URL: https://firdovsimammedovk.github.io/tenstorrent-sandbox
      DOCS_ID_NAMESPACE: tenstorrent
      DOCS_INDEX_VERSION: latest
      DOCS_OUTPUT_DIR: output
      DOCS_SEARCH_INGEST_API_KEY: ${{ secrets.DOCS_SEARCH_INGEST_API_KEY_DOCS_TENSTORRENT }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install the wheel
        run: |
          sudo apt-get update -y
          sudo apt-get install -y pandoc
          sudo apt install software-properties-common build-essential python3.12-venv libhwloc-dev graphviz
      - name: Install dependencies
        run: |
          python3 -m venv .docs-env
          source .docs-env/bin/activate
          pip install -r requirements.txt

      - name: Fetch latest versions from GitHub releases
        run: |
          # Fetch latest tt-kmd version
          KMD_VERSION=$(curl -s https://api.github.com/repos/tenstorrent/tt-kmd/releases/latest | jq -r '.tag_name' | sed 's/^ttkmd-//')
          echo "Latest tt-kmd version: $KMD_VERSION"
          echo "$KMD_VERSION" > syseng/kmd.version

          # Fetch latest tt-system-firmware version
          FW_VERSION=$(curl -s https://api.github.com/repos/tenstorrent/tt-system-firmware/releases/latest | jq -r '.tag_name' | sed 's/^v//')
          echo "Latest tt-system-firmware version: $FW_VERSION"
          echo "$FW_VERSION" > syseng/firmware.version

          # Fetch latest tt-system-tools version
          SYS_TOOLS_VERSION=$(curl -s https://api.github.com/repos/tenstorrent/tt-system-tools/releases/latest | jq -r '.tag_name' | sed 's/^v//')
          echo "Latest tt-system-tools version: $SYS_TOOLS_VERSION"
          echo "$SYS_TOOLS_VERSION" > syseng/sys_tools.version

      - name: Build documentation
        run: |
          source .docs-env/bin/activate
          python build_docs.py
      - name: Make root index.html and nojekyll
        run: |
          touch output/index.html
          touch output/.nojekyll
          echo -n "docs.tenstorrent.com" > output/CNAME
      - name: Warn if remote search ingest key is missing
        if: ${{ env.DOCS_SEARCH_INGEST_API_KEY == '' }}
        run: |
          echo "::warning::Skipping OpenSearch indexing because DOCS_SEARCH_INGEST_API_KEY_DOCS_TENSTORRENT is not set."
      - name: Index docs into OpenSearch
        if: ${{ env.DOCS_SEARCH_INGEST_API_KEY != '' }}
        run: |
          source .docs-env/bin/activate
          python scripts/index_remote_search.py
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          publish_branch: gh-pages
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output/
          force_orphan: true
